name: storage
description: Dart package which will manage sqflite as a database
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  music_repository:
    path: ../music_repository
  hive: ^2.0.4
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.0
  mocktail: ^0.1.1
  very_good_analysis: ^2.1.0